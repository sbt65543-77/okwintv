version: "3.9"

services:
  nextjs:
    container_name: client-lives
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: always
